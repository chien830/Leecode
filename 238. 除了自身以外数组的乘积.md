# 238. 除自身以外数组的乘积

## 题目描述

给你一个整数数组 `nums`，返回数组 `answer`，其中：

```
answer[i] = nums 中除了 nums[i] 之外其余各元素的乘积
```

题目保证：

- 数组中任意元素的全部前缀元素和后缀元素的乘积都在 32 位整数范围内；
- 不使用除法；
- 时间复杂度要求为 `O(n)`。

---

# 方法一：左右乘积列表

## 思想

对于每一个位置 `i`：

```
answer[i] = 左侧所有元素乘积 × 右侧所有元素乘积
```

因此可以提前计算：

- `left[i]`：第 `i` 个元素左侧所有元素的乘积；
- `right[i]`：第 `i` 个元素右侧所有元素的乘积。

最后：

```
answer[i] = left[i] * right[i]
```

---

## 代码

```cpp
class Solution {
public:
    vector<int> productExceptSelf(vector<int>& nums) {

        int n = size(nums);

        vector<int> answer(n, 1);
        vector<int> left(n, 1);
        vector<int> right(n, 1);


        // 计算左侧乘积
        for (int i = 1; i < n; i++)
        {
            left[i] = left[i-1] * nums[i-1];
        }


        // 计算右侧乘积
        for (int j = n-2; j >= 0; j--)
        {
            right[j] = right[j+1] * nums[j+1];
        }


        // 左侧乘积 × 右侧乘积
        for (int k = 0; k < n; k++)
        {
            answer[k] = left[k] * right[k];
        }

        return answer;
    }
};
```

---

# 方法二：answer 代替 left

## 思想

观察：

```
answer[i] = left[i] * right[i]
```

其中：

- `answer` 可以直接保存左侧乘积；
- 再使用 `right` 数组保存右侧乘积。

---

## 代码

```cpp
class Solution {
public:
    vector<int> productExceptSelf(vector<int>& nums) {

        int n = size(nums);

        vector<int> answer(n, 1);
        vector<int> right(n, 1);


        // answer 保存左侧乘积
        for (int i = 1; i < n; i++)
        {
            answer[i] = answer[i-1] * nums[i-1];
        }


        // 计算右侧乘积
        for (int j = n-2; j >= 0; j--)
        {
            right[j] = right[j+1] * nums[j+1];
        }


        // 左侧乘积 × 右侧乘积
        for (int k = 0; k < n; k++)
        {
            answer[k] = answer[k] * right[k];
        }

        return answer;
    }
};
```

---

# 方法三：进一步优化空间

## 思想

继续优化：

- 不保存整个 `right` 数组；
- 使用变量 `r` 保存当前右侧乘积。

此时：

```
answer[i] = 左侧乘积 × 当前右侧乘积
```

---

## 代码

```cpp
class Solution {
public:
    vector<int> productExceptSelf(vector<int>& nums) {

        int n = size(nums);

        vector<int> answer(n, 1);


        // answer 保存左侧乘积
        for (int i = 1; i < n; i++)
        {
            answer[i] = answer[i-1] * nums[i-1];
        }


        // r 保存右侧乘积
        int r = 1;

        for (int k = n-1; k >= 0; k--)
        {
            answer[k] = answer[k] * r;

            r *= nums[k];
        }

        return answer;
    }
};
```

---

# 核心思想总结

## 前缀积思想

题目特点：

```
每个位置需要其他所有元素的信息
```

如果直接计算：

```
每个位置重新遍历数组
```

时间复杂度：

```
O(n²)
```

---

优化：

```
把重复计算的信息提前存起来
```

利用：

```
前缀乘积 + 后缀乘积
```

将时间复杂度降低到：

```
O(n)
```

---

# 思考过程

```
当前位置答案

        ↓

拆分为：

左侧所有元素乘积
+
右侧所有元素乘积

        ↓

提前保存左右信息

        ↓

前缀 / 后缀思想
```

---

# 空间优化过程

```
左右乘积数组

        ↓

answer 代替 left

        ↓

变量 r 代替 right

        ↓

空间复杂度 O(1)
```
