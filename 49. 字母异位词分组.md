# LeetCode 49：字母异位词分组

## 题目

给你一个字符串数组，请你将 **字母异位词** 组合在一起。可以按任意顺序返回结果列表。

### 示例 1

```text
输入：
strs = ["eat", "tea", "tan", "ate", "nat", "bat"]

输出：
[["bat"], ["nat","tan"], ["ate","eat","tea"]]
```

解释：

- `"nat"` 和 `"tan"` 是字母异位词。
- `"ate"`、`"eat"` 和 `"tea"` 是字母异位词。
- `"bat"` 没有其他字母异位词。

### 示例 2

```text
输入：
strs = [""]

输出：
[[""]]
```

### 示例 3

```text
输入：
strs = ["a"]

输出：
[["a"]]
```

---

# 我的解法

## 思路

先把每个字符串进行排序。

例如：

```text
eat -> aet
tea -> aet
ate -> aet

tan -> ant
nat -> ant

bat -> abt
```

排序后相同，说明它们属于同一组。

之后使用 `num` 数组记录每个字符串属于第几组。

例如：

```text
strs = ["eat", "tea", "tan", "ate", "nat", "bat"]

num  = [1,     1,     2,     1,     2,     3]
```

表示：

```text
eat -> 第 1 组
tea -> 第 1 组
tan -> 第 2 组
ate -> 第 1 组
nat -> 第 2 组
bat -> 第 3 组
```

## 代码

```cpp
class Solution {
public:
    vector<vector<string>> groupAnagrams(vector<string>& strs) {

        vector<string> a(strs.size());
        vector<int> num(strs.size(), 0);

        // 1. 对每个字符串排序
        for(int i = 0; i < size(strs); i++)
        {
            a[i] = strs[i];
            sort(a[i].begin(), a[i].end());
        }

        // 2. 给相同的字符串分组
        int r = 1;

        for(int j = 0; j < size(strs); j++)
        {
            if(num[j] == 0)
            {
                for(int k = 0; k < size(strs); k++)
                {
                    if(a[j] == a[k])
                    {
                        num[k] = r;
                    }
                }

                r++;
            }
        }

        // 3. 根据组号构造结果
        vector<vector<string>> result;

        for(int m = 1; m < r; m++)
        {
            vector<string> mid;

            for(int n = 0; n < size(strs); n++)
            {
                if(num[n] == m)
                {
                    mid.push_back(strs[n]);
                }
            }

            result.push_back(mid);
        }

        return result;
    }
};
```

---

# 官方解法

```cpp
class Solution {
public:
    vector<vector<string>> groupAnagrams(vector<string>& strs) {

        unordered_map<string, vector<string>> mp;

        for(string& str : strs)
        {
            string key = str;

            sort(key.begin(), key.end());

            mp[key].emplace_back(str);
        }

        vector<vector<string>> ans;

        for(auto it = mp.begin(); it != mp.end(); ++it)
        {
            ans.emplace_back(it->second);
        }

        return ans;
    }
};
```

---

# 官方解法核心

```cpp
unordered_map<string, vector<string>> mp;
```

其中：

```text
key   = 排序后的字符串
value = 属于这一组的原字符串
```

例如：

```text
"aet" -> ["eat", "tea", "ate"]

"ant" -> ["tan", "nat"]

"abt" -> ["bat"]
```

核心代码：

```cpp
string key = str;

sort(key.begin(), key.end());

mp[key].emplace_back(str);
```

可以理解为：

```text
复制原字符串
↓
排序
↓
排序结果作为 key
↓
把原字符串放入这一组
```

例如：

```cpp
str = "eat";
key = "aet";

mp["aet"].push_back("eat");
```

之后：

```cpp
str = "tea";
key = "aet";

mp["aet"].push_back("tea");
```

得到：

```text
"aet" -> ["eat", "tea"]
```

---

# 复习 1：push_back

作用：

> 在 `vector` 最后添加一个元素。

例如：

```cpp
vector<int> v;

v.push_back(10);
v.push_back(20);
```

结果：

```text
v = [10, 20]
```

字符串：

```cpp
vector<string> v;

v.push_back("eat");
v.push_back("tea");
```

结果：

```text
v = ["eat", "tea"]
```

---

# 复习 2：emplace_back

作用和 `push_back` 很像，也是：

> 在 `vector` 最后添加元素。

例如：

```cpp
vector<int> v;

v.emplace_back(10);
v.emplace_back(20);
```

---

# push_back 和 emplace_back

简单记：

```text
push_back
=
把已经存在的对象放进去

emplace_back
=
直接在 vector 中构造对象
```

普通情况下：

```cpp
vector<string> v;

string s = "hello";

v.push_back(s);
v.emplace_back(s);
```

二者效果基本一样。

复杂类型例如：

```cpp
vector<pair<int, string>> v;
```

`push_back`：

```cpp
v.push_back({1, "apple"});
```

`emplace_back`：

```cpp
v.emplace_back(1, "apple");
```

---

# 一眼复习

### 字母异位词

```text
eat -> aet
tea -> aet
ate -> aet
```

排序后相同，所以属于同一组。

### 官方核心

```cpp
unordered_map<string, vector<string>> mp;
```

```cpp
string key = str;
sort(key.begin(), key.end());
mp[key].push_back(str);
```

理解：

```text
排序后的字符串
        ↓
      key
        ↓
相同 key 的字符串放到一起
```

### push_back

```cpp
v.push_back(x);
```

表示：

```text
把 x 添加到 vector 最后
```

### emplace_back

```cpp
v.emplace_back(x);
```

简单情况下可以先理解为：

```text
和 push_back 类似
```
