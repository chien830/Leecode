# 二叉树翻转中的递归思想

## 题目

给你一棵二叉树的根节点 `root`，翻转这棵二叉树，并返回其根节点。

示例：

原始：

``` text
        4
       / \
      2   7
     / \ / \
    1  3 6  9
```

翻转后：

``` text
        4
       / \
      7   2
     / \ / \
    9  6 3  1
```

------------------------------------------------------------------------

## 代码

``` cpp
class Solution {
public:
    TreeNode* invertTree(TreeNode* root) {

        if(root == nullptr)
        {
            return root;
        }

        invertTree(root->left);

        invertTree(root->right);

        swap(root->left, root->right);

        return root;
    }
};
```

------------------------------------------------------------------------

# 递归思想

## 递归假设（Recursive Hypothesis）

人脑习惯：

> 一步一步执行代码。

例如：

``` text
第一行做什么
第二行做什么
第三行做什么
```

但是递归要求你换一种思考方式：

> 假设函数已经解决了更小规模的问题，我只需要解决当前这一层。

------------------------------------------------------------------------

# 一个关键比喻：递归像管理任务

假设老板需要：

    计算整个公司的员工数量

公司的结构：

``` text
老板
 |
 +--部门A
 |
 +--部门B
```

老板不会自己去统计每个人。

老板会说：

> 部门A负责人，你统计你的部门。

部门A负责人继续：

``` text
部门A
 |
 +--小组1
 |
 +--小组2
```

然后继续向下分配任务。

最后结果返回。

老板只需要：

    自己1人
    +
    部门A人数
    +
    部门B人数

------------------------------------------------------------------------

老板不需要知道：

> 部门A里面具体怎么统计。

这就是：

> 假设子问题已经解决。

------------------------------------------------------------------------

# 在二叉树中的理解

例如：

``` text
        4
       / \
      2   7
```

执行：

``` cpp
invertTree(root)
```

函数的含义应该定义为：

> 翻转以 root 为根节点的整棵树，并返回翻转后的根节点。

------------------------------------------------------------------------

现在站在节点4。

它有两个子问题：

左子树：

``` cpp
invertTree(root->left)
```

右子树：

``` cpp
invertTree(root->right)
```

我们假设：

左子树已经翻转完成。

右子树已经翻转完成。

那么当前节点4只需要：

交换左右孩子：

``` cpp
swap(root->left, root->right);
```

------------------------------------------------------------------------

# 为什么可以假设子问题已经解决？

因为递归函数有一个承诺：

例如：

``` cpp
invertTree(root)
```

表示：

> 给我任意一棵树，我都会把它翻转。

那么：

``` cpp
invertTree(root->left)
```

也是同一个函数。

它同样能够：

> 翻转左子树。

所以我们不需要关心：

左子树内部如何翻转。

只需要相信：

它会返回正确结果。

------------------------------------------------------------------------

# 递归三个关键问题

## 1. 函数代表什么？

这是最重要的问题。

错误：

    maxDepth(root)
    表示访问root节点

这样无法递归。

正确：

    maxDepth(root)
    表示返回root这棵树的高度

那么：

    maxDepth(root->left)

自然表示：

    左子树高度

------------------------------------------------------------------------

## 2. 递归结束条件是什么？

递归必须有出口。

二叉树中：

空节点：

``` cpp
root == nullptr
```

所以：

``` cpp
if(root == nullptr)
{
    return root;
}
```

------------------------------------------------------------------------

## 3. 当前节点如何利用子问题结果？

对于翻转二叉树：

子问题：

    左子树翻转完成
    右子树翻转完成

当前节点：

    交换左右节点

因此：

``` cpp
swap(root->left, root->right);
```

------------------------------------------------------------------------

# 总结

递归不是：

> 不断调用自己。

递归真正的思想是：

    定义一个函数
            ↓
    相信它能够解决更小的问题
            ↓
    当前节点只处理自己的事情
            ↓
    返回结果

写递归时始终问：

1.  我的函数表示什么？
2.  最简单情况是什么？
3.  如果左右子问题已经解决，我当前节点怎么办？

理解这三个问题，就能写出大部分二叉树递归代码。
