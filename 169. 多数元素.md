# 169. 多数元素

给定一个大小为 n 的数组 nums ，返回其中的多数元素。

多数元素是指在数组中出现次数 **大于 ⌊ n/2 ⌋** 的元素。

你可以假设数组是非空的，并且给定的数组总是存在多数元素。


## 代码

```cpp
class Solution {
public:
    int majorityElement(vector<int>& nums) {

        unordered_map<int,int> hash;

        for(int i=0;i<size(nums);i++)
        {
            hash[nums[i]]++;
        }

        for(auto pair : hash) 
        {
            // 依次取出 hash 中的每一个元素，并赋值给 pair

            if(pair.second > size(nums)/2)
            {
                return pair.first;
            }
        }

        return 0;
    }
};
```

---

## 哈希表选择

```
范围小 → vector<int> 计数

范围大 → unordered_map<int,int> 哈希表
```

---

## for(auto pair : hash)

```cpp
for(auto pair : hash)
{
    if(pair.second > nums.size()/2)
    {
        return pair.first;
    }
}
```

说明：

```cpp
for(auto pair : hash)
```

依次取出 hash 中的每一个元素，并赋值给 pair。


---

## 对象访问 vs 指针访问


### pair.first / pair.second

`.` 点号：对象直接访问成员

```cpp
pair.first
pair.second
```


### it->first / it->second

`->` 箭头：指针访问成员

```cpp
it->first
it->second
```

这里的 `it` 不是一个对象，而是一个指针（更准确说是迭代器）。



# 方法二：排序思路

如果将数组 `nums` 中的所有元素按照单调递增或单调递减的顺序排序，那么下标为：

```
⌊ n / 2 ⌋
```

的元素（下标从 0 开始）一定是众数。


## 代码

```cpp
class Solution {
public:
    int majorityElement(vector<int>& nums) {

        sort(nums.begin(), nums.end());

        return nums[nums.size() / 2];
    }
};
```


#### 方法三：分治

**思路**

如果数 `a` 是数组 `nums` 的众数，那么：

如果我们将 `nums` 分成两部分，`a` 必定是至少一部分的众数。

